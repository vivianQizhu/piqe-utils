"""Module to perform basic operations(life cycle e.g.) on the domain"""

class DomainBasicOp(object):

    def __init__(self, dom):
        self.dom = dom

    def reboot(self):

    def graceful_shutdown(self):